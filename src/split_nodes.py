def split_nodes_delimiter(old_nodes, delimiter, text_type):
    #takes a list of "old nodes", a delimiter, and a text type. 
    # It should return a new list of nodes, 
    # where any "text" type nodes in the input list 
    # are (potentially) split into multiple nodes based on the syntax.
    print(f"SPLIT NODES DELIMETER FUNCTION: {old_nodes}")